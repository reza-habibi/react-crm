import axios from "axios";
import { TAddTicket } from "../../types.ds";

import {
  addTicketPending,
  addTicketSuccess,
  addTicketFail,
} from "./addTicketSlice";


const addTicket = (formData:TAddTicket)=>{
    return new Promise(async (resolve , reject)=>{
        try {
            
        } catch (error) {
            
        }
    })
}